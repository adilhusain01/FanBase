# FanBase Audit MCP

Read-only MCP server for support and audit agents. It deliberately has no tools to issue passes, modify allocations, fetch raw social evidence, or move funds.

```bash
pnpm install
pnpm build
pnpm start
```

Tools: `get_fairness_policy`, `verify_score_commitment`, and `get_live_world_cup_match`.
